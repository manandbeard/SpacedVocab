## Packages
recharts | For visualizing student progress and teacher dashboard analytics
framer-motion | For smooth page transitions and quiz card flip animations
date-fns | For formatting dates in tables and charts

## Notes
Replit Auth is pre-configured; use window.location.href = "/api/login" for auth.
Teacher and Student roles are currently shared (any user can access both views for MVP).
Use 'Outfit' for headings and 'Inter' for body text.
Dark mode is the default and primary aesthetic.
